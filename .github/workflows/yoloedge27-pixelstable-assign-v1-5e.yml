name: YOLOEdge27 Pixel-Stable Assignment v1 5e

on:
  push:
    branches:
      - research/yoloedge27-pixelstable-assign-v1
    paths:
      - .github/workflows/yoloedge27-pixelstable-assign-v1-5e.yml
      - scripts/run_pixel_stable_assignment_test.py
      - scripts/sanity.py
      - src/pixel_stable_assigner.py
      - src/ultralytics_patch.py
      - tests/test_pixel_stable_assigner.py
  workflow_dispatch:

concurrency:
  group: yoloedge27-pixelstable-assign-v1-5e
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  pixelstable-v1-5e:
    runs-on: [self-hosted, Linux, X64, gpu, rtx4060ti, visdrone]
    timeout-minutes: 180
    env:
      CUDA_VISIBLE_DEVICES: "0"
      PYTHON_BIN: /home/minh/miniconda3/envs/torch251/bin/python
      DATASET: /home/minh/Desktop/project/visdrone_local_project/dataset
      SCREEN_ROOT: /home/minh/yoloedge27_experiments/stage4/pixelstable_assign_v1_5e
      REPORT_DIR: reports/yoloedge27/stage4/pixelstable_assign_v1_5e
      PYTHONPATH: ${{ github.workspace }}/third_party/ultralytics:${{ github.workspace }}

    steps:
      - name: Checkout pixel-stable assignment branch
        uses: actions/checkout@v4
        with:
          ref: research/yoloedge27-pixelstable-assign-v1
          fetch-depth: 0

      - name: Verify mechanism and controls
        shell: bash
        run: |
          set -euo pipefail
          echo "HEAD=$(git rev-parse HEAD)"
          git log -1 --oneline
          grep -q 'class TinyPixelStableAssigner' src/pixel_stable_assigner.py
          grep -q 'def pixel_stable_quality' src/pixel_stable_assigner.py
          grep -q 'TinyPixelStableAssigner' src/ultralytics_patch.py
          grep -q 'Criterion assigner:' scripts/run_pixel_stable_assignment_test.py
          grep -q '"pixel_stable": "TinyPixelStableAssigner"' scripts/sanity.py
          test -f reports/yoloedge27/stage3/head_snr_v1_5e/summary.json

      - name: Pre-flight GPU and dataset
        shell: bash
        run: |
          set -euo pipefail
          nvidia-smi
          "$PYTHON_BIN" - <<'PY'
          import torch
          print('Torch:', torch.__version__)
          print('CUDA:', torch.version.cuda)
          print('CUDA available:', torch.cuda.is_available())
          assert torch.cuda.is_available()
          print('GPU:', torch.cuda.get_device_name(0))
          PY
          test -d "$DATASET/VisDrone2019-DET-train/images"
          test -d "$DATASET/VisDrone2019-DET-val/images"

      - name: Bootstrap local config
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$SCREEN_ROOT"
          mkdir -p "$SCREEN_ROOT" "$REPORT_DIR"
          cat > config/local.yaml <<EOF
          dataset_root: "$DATASET"
          dataset_format: "visdrone_official"
          train_images: "VisDrone2019-DET-train/images"
          train_annotations: "VisDrone2019-DET-train/annotations"
          val_images: "VisDrone2019-DET-val/images"
          val_annotations: "VisDrone2019-DET-val/annotations"
          test_images: "VisDrone2019-DET-test-dev/images"
          test_annotations: "VisDrone2019-DET-test-dev/annotations"
          test_image: ""
          runs_dir: "$SCREEN_ROOT/runs"
          state_dir: "$SCREEN_ROOT/state"
          outputs_dir: "$SCREEN_ROOT/outputs"
          generated_dir: "$SCREEN_ROOT/generated"
          EOF

      - name: Setup pinned dependencies
        shell: bash
        run: |
          set -euo pipefail
          PYTHON_BIN="$PYTHON_BIN" bash setup_local.sh
          "$PYTHON_BIN" - <<'PY'
          from pathlib import Path
          import ultralytics
          expected = (Path.cwd() / 'third_party' / 'ultralytics').resolve()
          print('Ultralytics:', ultralytics.__version__)
          print('Source:', ultralytics.__file__)
          assert Path(ultralytics.__file__).resolve().is_relative_to(expected)
          PY

      - name: Unit tests
        shell: bash
        env:
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
        run: |
          set -euo pipefail
          "$PYTHON_BIN" -m pytest -q \
            tests/test_sprdown.py \
            tests/test_pixel_stable_assigner.py

      - name: Train only P1 pixel-stable assignment student
        shell: bash
        run: |
          set -euo pipefail
          "$PYTHON_BIN" scripts/run_pixel_stable_assignment_test.py \
            --dataset "$DATASET" \
            --screen-root "$SCREEN_ROOT" \
            --report-dir "$REPORT_DIR" \
            --epochs 5 \
            --batch 8 \
            --nbs 16 \
            --workers 4 \
            --tiny-threshold 16 \
            --perturb-px 1

      - name: Commit compact pixel-stable reports
        if: ${{ always() }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$REPORT_DIR"
          {
            echo "branch=research/yoloedge27-pixelstable-assign-v1"
            echo "head=$(git rev-parse HEAD)"
            echo "screen_root=$SCREEN_ROOT"
            echo "student=H1 direct reg1"
            echo "variant=P1 pixel-stable tiny assignment"
            echo "epochs=5"
            echo "tiny_threshold_px=16"
            echo "perturb_px=1"
            echo "inference_change=none"
            echo "training_extra=four IoU perturbation evaluations inside assignment"
            echo "note=local mechanism screening only"
          } > "$REPORT_DIR/workflow_meta.txt"

          git checkout -- config/experiment.yaml || true
          git status --short
          git config user.name "yoloedge27-runner"
          git config user.email "actions@users.noreply.github.com"
          git add "$REPORT_DIR"
          if git diff --cached --quiet; then
            echo "No pixel-stable report changes to commit"
            exit 0
          fi
          git commit -m "Record pixel-stable assignment v1 5e screening"
          git pull --rebase origin research/yoloedge27-pixelstable-assign-v1
          git push origin HEAD:research/yoloedge27-pixelstable-assign-v1
